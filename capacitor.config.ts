import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suhendararyadi.absensiku',
  appName: 'AbsensiKu',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
